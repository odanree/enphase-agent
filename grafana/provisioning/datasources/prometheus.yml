# Provisioned datasource — pinned uid so the dashboard JSON can reference
# it stably instead of depending on click-through setup.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
